# Security

Do not commit API keys, credentials, access tokens, or real client data to this repository. Keep local credentials in an uncommitted `.env` file copied from `.env.example`.

If an API key or other secret is accidentally committed, rotate or revoke it immediately with the provider, remove it from the repository, and review the commit history before sharing the project.
